pipeline {
  stages {

    stage('prep') {
      println 'prep'
    }

    stage('build') {
      println 'build'
    }

    stage('test') {
      println 'test'
    }

    stage('deploy') {
      println 'deploy'
    }
  }
}